تمام! سأعطيك جولة تعليمية احترافية ومحسّنة بالكامل 🎯
📁 الملفات المطلوبة:
1️⃣ ملف المكون الرئيسي
components/OnboardingTour.jsx
jsximport { useState, useEffect } from 'react';
import { X, ArrowRight, ArrowLeft, Sparkles, Target, Zap, Palette, Download } from 'lucide-react';

const TOUR_STEPS = [
  {
    id: 1,
    title: "مرحباً بك في رقيم AI 966 🎉",
    description: "منصتك الذكية لتوليد صور احترافية بالذكاء الاصطناعي - دعنا نأخذك في جولة سريعة",
    icon: Sparkles,
    highlight: null,
    position: "center"
  },
  {
    id: 2,
    title: "اكتب وصفك هنا ✍️",
    description: "صف الصورة التي تريدها بالتفصيل - كلما كان الوصف أدق، كانت النتيجة أفضل",
    icon: Target,
    highlight: "#prompt-input",
    position: "bottom"
  },
  {
    id: 3,
    title: "اختر النمط المناسب 🎨",
    description: "واقعي للصور الطبيعية، فني للتصاميم الإبداعية، أو قياسي للاستخدام العام",
    icon: Palette,
    highlight: "#style-selector",
    position: "bottom"
  },
  {
    id: 4,
    title: "حدد مستوى الجودة ⚡",
    description: "عالية للمشاريع المهمة، فائقة للطباعة والاستخدام التجاري، قياسية للتجربة السريعة",
    icon: Zap,
    highlight: "#quality-selector",
    position: "bottom"
  },
  {
    id: 5,
    title: "اضغط لتوليد الصورة 🚀",
    description: "جاهز؟ اضغط على زر 'ولّد الآن' واحصل على صورتك في ثوانٍ",
    icon: Sparkles,
    highlight: "#generate-button",
    position: "top"
  },
  {
    id: 6,
    title: "استخدم القوالب الجاهزة 💡",
    description: "لا تبدأ من الصفر! تصفح الأمثلة السريعة أدناه وانسخ البرومبت مباشرة",
    icon: Target,
    highlight: "#quick-examples",
    position: "top"
  },
  {
    id: 7,
    title: "حمّل وشارك نتائجك 📥",
    description: "بعد التوليد، يمكنك تحميل الصورة أو مشاركتها مباشرة",
    icon: Download,
    highlight: "#download-section",
    position: "top"
  },
  {
    id: 8,
    title: "هل أنت جاهز؟ 🎯",
    description: "رائع! الآن أنت تعرف كل شيء - ابدأ بإنشاء صورتك الأولى",
    icon: Sparkles,
    highlight: null,
    position: "center"
  }
];

export default function OnboardingTour({ onComplete }) {
  const [currentStep, setCurrentStep] = useState(0);
  const [isVisible, setIsVisible] = useState(false);

  useEffect(() => {
    // تحقق إذا كان المستخدم شاف الجولة قبل كذا
    const hasSeenTour = localStorage.getItem('raqim-tour-completed');
    if (!hasSeenTour) {
      setTimeout(() => setIsVisible(true), 500);
    }
  }, []);

  const currentStepData = TOUR_STEPS[currentStep];
  const Icon = currentStepData?.icon;

  const handleNext = () => {
    if (currentStep < TOUR_STEPS.length - 1) {
      setCurrentStep(currentStep + 1);
    } else {
      completeTour();
    }
  };

  const handlePrev = () => {
    if (currentStep > 0) {
      setCurrentStep(currentStep - 1);
    }
  };

  const skipTour = () => {
    setIsVisible(false);
    localStorage.setItem('raqim-tour-completed', 'true');
    onComplete?.();
  };

  const completeTour = () => {
    setIsVisible(false);
    localStorage.setItem('raqim-tour-completed', 'true');
    onComplete?.();
  };

  if (!isVisible) return null;

  return (
    <>
      {/* Overlay */}
      <div className="fixed inset-0 bg-black/60 backdrop-blur-sm z-[9998] animate-fadeIn" />

      {/* Modal */}
      <div className="fixed inset-0 z-[9999] flex items-center justify-center p-4">
        <div className="bg-white rounded-3xl shadow-2xl max-w-lg w-full animate-slideUp">
          
          {/* Header */}
          <div className="relative p-6 pb-4">
            <button
              onClick={skipTour}
              className="absolute left-4 top-4 p-2 hover:bg-gray-100 rounded-full transition-colors"
            >
              <X className="w-5 h-5 text-gray-500" />
            </button>
            
            <div className="flex items-center justify-center mb-4">
              <div className="w-16 h-16 bg-gradient-to-br from-blue-500 to-purple-600 rounded-2xl flex items-center justify-center shadow-lg">
                {Icon && <Icon className="w-8 h-8 text-white" />}
              </div>
            </div>
          </div>

          {/* Content */}
          <div className="px-6 pb-6 text-center">
            <h2 className="text-2xl font-bold text-gray-800 mb-3">
              {currentStepData.title}
            </h2>
            <p className="text-gray-600 leading-relaxed text-lg mb-6">
              {currentStepData.description}
            </p>

            {/* Progress Dots */}
            <div className="flex items-center justify-center gap-2 mb-6">
              {TOUR_STEPS.map((_, index) => (
                <div
                  key={index}
                  className={`h-2 rounded-full transition-all duration-300 ${
                    index === currentStep
                      ? 'w-8 bg-gradient-to-r from-blue-500 to-purple-600'
                      : index < currentStep
                      ? 'w-2 bg-green-500'
                      : 'w-2 bg-gray-300'
                  }`}
                />
              ))}
            </div>

            {/* Action Buttons */}
            <div className="flex gap-3">
              {currentStep > 0 && (
                <button
                  onClick={handlePrev}
                  className="flex-1 px-6 py-3 bg-gray-100 hover:bg-gray-200 text-gray-700 rounded-xl font-medium transition-colors flex items-center justify-center gap-2"
                >
                  <ArrowRight className="w-5 h-5 rotate-180" />
                  السابق
                </button>
              )}
              
              <button
                onClick={handleNext}
                className="flex-1 px-6 py-3 bg-gradient-to-r from-blue-500 to-purple-600 hover:from-blue-600 hover:to-purple-700 text-white rounded-xl font-bold transition-all shadow-lg hover:shadow-xl flex items-center justify-center gap-2"
              >
                {currentStep === TOUR_STEPS.length - 1 ? (
                  <>
                    <Sparkles className="w-5 h-5" />
                    ابدأ الآن
                  </>
                ) : (
                  <>
                    التالي
                    <ArrowLeft className="w-5 h-5" />
                  </>
                )}
              </button>
            </div>

            {/* Skip Button */}
            {currentStep < TOUR_STEPS.length - 1 && (
              <button
                onClick={skipTour}
                className="mt-4 text-sm text-gray-500 hover:text-gray-700 transition-colors"
              >
                تخطي الجولة
              </button>
            )}

            {/* Step Counter */}
            <p className="mt-4 text-xs text-gray-400">
              الخطوة {currentStep + 1} من {TOUR_STEPS.length}
            </p>
          </div>
        </div>
      </div>
    </>
  );
}

// ═══════════════════════════════════════════════════════════
// زر إعادة تشغيل الجولة (اختياري)
// ═══════════════════════════════════════════════════════════

export function RestartTourButton() {
  const restartTour = () => {
    localStorage.removeItem('raqim-tour-completed');
    window.location.reload();
  };

  return (
    <button
      onClick={restartTour}
      className="fixed bottom-6 left-6 px-4 py-2 bg-blue-500 hover:bg-blue-600 text-white rounded-full shadow-lg flex items-center gap-2 text-sm font-medium transition-all hover:scale-105 z-50"
    >
      <Sparkles className="w-4 h-4" />
      ابدأ الجولة التعريفية
    </button>
  );
}

2️⃣ استخدام المكون في الصفحة الرئيسية
في app/page.jsx أو pages/index.jsx:
jsximport { useState } from 'react';
import OnboardingTour, { RestartTourButton } from '@/components/OnboardingTour';

export default function Home() {
  const [tourCompleted, setTourCompleted] = useState(false);

  return (
    <div className="min-h-screen">
      {/* الجولة التعليمية */}
      <OnboardingTour onComplete={() => setTourCompleted(true)} />

      {/* المحتوى الموجود */}
      <div id="prompt-input">
        {/* حقل الإدخال */}
      </div>

      <div id="style-selector">
        {/* اختيار النمط */}
      </div>

      <div id="quality-selector">
        {/* اختيار الجودة */}
      </div>

      <button id="generate-button">
        {/* زر التوليد */}
      </button>

      <div id="quick-examples">
        {/* الأمثلة السريعة */}
      </div>

      {/* زر إعادة الجولة */}
      <RestartTourButton />
    </div>
  );
}

3️⃣ إضافة الأنيميشن في Tailwind
في tailwind.config.js:
jsmodule.exports = {
  theme: {
    extend: {
      animation: {
        'fadeIn': 'fadeIn 0.3s ease-in-out',
        'slideUp': 'slideUp 0.4s ease-out',
      },
      keyframes: {
        fadeIn: {
          '0%': { opacity: '0' },
          '100%': { opacity: '1' },
        },
        slideUp: {
          '0%': { opacity: '0', transform: 'translateY(20px)' },
          '100%': { opacity: '1', transform: 'translateY(0)' },
        },
      },
    },
  },
}

✨ المميزات:
✅ تصميم عصري وجذاب
✅ أنيميشن سلس واحترافي
✅ شرح واضح لكل خطوة
✅ Progress indicators
✅ يحفظ الحالة في localStorage
✅ زر لإعادة تشغيل الجولة
✅ RTL friendly
✅ Responsive