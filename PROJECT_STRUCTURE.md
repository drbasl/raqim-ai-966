# 📁 هيكل مشروع رقيم AI 966

## نظرة عامة

**رقيم AI 966** هو موقع ويب عربي متكامل لتوليد البرومبتات الذكية للذكاء الاصطناعي، مبني بتقنيات حديثة مع دعم كامل للغة العربية واتجاه RTL.

---

## 🏗️ البنية الأساسية

```
raqim_ai_966/
├── client/                 # تطبيق الواجهة الأمامية (Frontend)
├── server/                 # تطبيق الخادم (Backend)
├── shared/                 # الكود المشترك بين Frontend و Backend
├── drizzle/                # قاعدة البيانات والهجرات (Migrations)
├── patches/                # تصحيحات المكتبات الخارجية
└── ملفات التكوين          # package.json, tsconfig.json, vite.config.ts
```

---

## 📂 الأقسام الرئيسية

### 1️⃣ **client/** - الواجهة الأمامية

#### **client/public/**
ملفات ثابتة يتم تقديمها مباشرة للمستخدم:
- `logo.svg` - شعار رقيم AI 966 السداسي الأزرق
- ملفات أخرى (favicon، robots.txt، إلخ)

#### **client/src/**
الكود المصدري للواجهة الأمامية:

##### **client/src/pages/**
صفحات التطبيق الرئيسية:
- `Home.tsx` - الصفحة الرئيسية تحتوي على:
  - قسم Hero مع الشعار المتحرك
  - أداة توليد البرومبتات
  - مكتبة القوالب
  - البرومبتات الشعبية
  - الأمثلة الحية
  - محلل البرومبتات
  - القوالب المتخصصة
  - الأسئلة الشائعة
- `MyLibrary.tsx` - صفحة المكتبة الشخصية لحفظ البرومبتات
- `NotFound.tsx` - صفحة 404
- `ComponentShowcase.tsx` - عرض المكونات (للتطوير)

##### **client/src/components/**
المكونات القابلة لإعادة الاستخدام:

**المكونات الرئيسية:**
- `PromptGenerator.tsx` - أداة توليد البرومبتات الذكية
- `TemplateLibrary.tsx` - مكتبة القوالب الجاهزة (6 قوالب)
- `PopularPrompts.tsx` - عرض البرومبتات الأكثر شعبية (8 برومبتات)
- `LiveExamples.tsx` - أمثلة حية واقعية (4 أمثلة)
- `PromptAnalyzer.tsx` - محلل البرومبتات الذكي
- `SpecializedTemplates.tsx` - القوالب المتخصصة (8 مجالات × 3 قوالب)
- `FAQ.tsx` - الأسئلة الشائعة (6 أسئلة تفاعلية)
- `ErrorBoundary.tsx` - معالج الأخطاء

**client/src/components/ui/**
مكونات واجهة المستخدم من shadcn/ui:
- `button.tsx` - أزرار بأنماط مختلفة
- `card.tsx` - بطاقات لعرض المحتوى
- `input.tsx` - حقول الإدخال
- `textarea.tsx` - حقول النص الكبيرة
- `select.tsx` - قوائم منسدلة
- `checkbox.tsx` - صناديق اختيار
- `badge.tsx` - شارات ملونة
- `dialog.tsx` - نوافذ منبثقة
- `accordion.tsx` - قوائم قابلة للطي
- `toast.tsx` / `sonner.tsx` - إشعارات
- `tooltip.tsx` - تلميحات
- وغيرها...

##### **client/src/contexts/**
سياقات React للحالة العامة:
- `ThemeContext.tsx` - إدارة الوضع الداكن/الفاتح مع LocalStorage

##### **client/src/hooks/**
Hooks مخصصة:
- `useComposition.ts` - معالجة إدخال اللغات المركبة
- `useMobile.tsx` - كشف الأجهزة المحمولة
- `usePersistFn.ts` - الحفاظ على مراجع الدوال

##### **client/src/lib/**
مكتبات مساعدة:
- `trpc.ts` - إعداد عميل tRPC للاتصال بالخادم
- `utils.ts` - دوال مساعدة عامة

##### **client/src/**
ملفات أخرى:
- `App.tsx` - المكون الجذر مع التوجيه (Routing)
- `main.tsx` - نقطة الدخول الرئيسية
- `index.css` - الأنماط العامة مع Tailwind CSS
- `const.ts` - الثوابت المشتركة (APP_TITLE, APP_LOGO)

---

### 2️⃣ **server/** - الخادم

#### **server/_core/**
البنية التحتية الأساسية للخادم:
- `index.ts` - نقطة دخول الخادم (Express + Vite)
- `trpc.ts` - إعداد tRPC router
- `context.ts` - سياق tRPC (المستخدم، الطلب، الاستجابة)
- `oauth.ts` - مصادقة Manus OAuth
- `cookies.ts` - إدارة ملفات تعريف الارتباط
- `env.ts` - متغيرات البيئة
- `llm.ts` - تكامل نماذج اللغة الكبيرة (LLM)
- `imageGeneration.ts` - توليد الصور بالذكاء الاصطناعي
- `voiceTranscription.ts` - تحويل الصوت إلى نص
- `map.ts` - تكامل خرائط Google
- `dataApi.ts` - الوصول إلى APIs خارجية
- `notification.ts` - إرسال إشعارات للمالك
- `systemRouter.ts` - مسارات النظام
- `vite.ts` - تكامل Vite مع Express

#### **server/_core/types/**
أنواع TypeScript:
- `manusTypes.ts` - أنواع خاصة بمنصة Manus
- `cookie.d.ts` - أنواع ملفات تعريف الارتباط

#### **server/**
ملفات الخادم الرئيسية:
- `routers.ts` - مسارات tRPC (توليد البرومبتات، التحليل، إلخ)
- `db.ts` - دوال قاعدة البيانات (Drizzle ORM)
- `storage.ts` - تخزين الملفات على S3

---

### 3️⃣ **shared/** - الكود المشترك

#### **shared/_core/**
- `errors.ts` - أنواع الأخطاء المخصصة

#### **shared/**
- `const.ts` - الثوابت المشتركة (COOKIE_NAME)
- `types.ts` - أنواع TypeScript المشتركة

---

### 4️⃣ **drizzle/** - قاعدة البيانات

#### **drizzle/**
- `schema.ts` - مخطط قاعدة البيانات (الجداول والعلاقات):
  - جدول `users` - المستخدمين
  - جدول `saved_prompts` - البرومبتات المحفوظة
  - جدول `popular_prompts` - البرومبتات الشعبية
- `relations.ts` - العلاقات بين الجداول
- `0000_*.sql` - ملفات الهجرة (Migrations)
- `0001_*.sql` - هجرات إضافية

#### **drizzle/meta/**
- `_journal.json` - سجل الهجرات
- `*_snapshot.json` - لقطات المخطط

#### **drizzle/migrations/**
- `.gitkeep` - مجلد الهجرات

---

### 5️⃣ **patches/** - التصحيحات

- `wouter@3.7.1.patch` - تصحيح مكتبة wouter للتوجيه

---

## 📄 ملفات التكوين الرئيسية

### **package.json**
تعريف المشروع والاعتماديات:
- **الاعتماديات الرئيسية:**
  - `react` - مكتبة واجهة المستخدم
  - `express` - إطار عمل الخادم
  - `@trpc/server` & `@trpc/client` - اتصال نوع آمن
  - `drizzle-orm` - ORM لقاعدة البيانات
  - `tailwindcss` - إطار عمل CSS
  - `lucide-react` - أيقونات
  - `wouter` - التوجيه
- **النصوص البرمجية (Scripts):**
  - `pnpm dev` - تشغيل الخادم التطويري
  - `pnpm build` - بناء المشروع للإنتاج
  - `pnpm db:push` - دفع تغييرات قاعدة البيانات

### **tsconfig.json**
تكوين TypeScript:
- دعم React JSX
- مسارات مختصرة (`@/` للوصول إلى `client/src/`)
- إعدادات صارمة للتحقق من الأنواع

### **vite.config.ts**
تكوين Vite:
- إعداد React plugin
- تكوين المسارات المختصرة
- إعدادات البناء والتطوير

### **drizzle.config.ts**
تكوين Drizzle ORM:
- اتصال قاعدة البيانات (MySQL/TiDB)
- مسارات المخطط والهجرات

### **components.json**
تكوين shadcn/ui:
- نمط المكونات
- ألوان النظام
- مسارات المكونات

---

## 🎨 نظام الألوان والتصميم

### **client/src/index.css**
الأنماط العامة والمتغيرات:

#### **الوضع الداكن (Dark Mode):**
- `--background: oklch(0.15 0.02 240)` - خلفية داكنة
- `--foreground: oklch(0.98 0.005 240)` - نص فاتح
- `--primary: oklch(0.6 0.2 230)` - أزرق محيطي (Ocean Blue)
- `--accent: oklch(0.65 0.22 200)` - سماوي للتمييز

#### **الوضع الفاتح (Light Mode):**
- `--background: oklch(0.98 0.005 240)` - خلفية فاتحة
- `--foreground: oklch(0.15 0.02 240)` - نص داكن
- `--primary: oklch(0.55 0.22 230)` - أزرق محيطي معدل
- `--accent: oklch(0.92 0.02 200)` - سماوي فاتح

#### **تأثيرات خاصة:**
- `.neon-glow` - توهج نيون أزرق
- `.neon-text` - نص متوهج
- أنيميشن `float`, `rotate-slow`, `pulse-glow`

---

## 🗄️ قاعدة البيانات

### **الجداول:**

#### **users**
معلومات المستخدمين:
- `id` - المعرف الفريد
- `openId` - معرف Manus OAuth
- `name`, `email` - معلومات الملف الشخصي
- `role` - دور المستخدم (admin/user)
- `createdAt`, `updatedAt`, `lastSignedIn` - طوابع زمنية

#### **saved_prompts**
البرومبتات المحفوظة للمستخدمين:
- `id` - المعرف الفريد
- `userId` - معرف المستخدم
- `title` - عنوان البرومبت
- `content` - محتوى البرومبت
- `usageType` - نوع الاستخدام
- `createdAt` - تاريخ الإنشاء

#### **popular_prompts**
البرومبتات الشعبية:
- `id` - المعرف الفريد
- `title` - عنوان البرومبت
- `description` - وصف مختصر
- `prompt` - نص البرومبت الكامل
- `category` - الفئة
- `rating` - التقييم (من 5)
- `usageCount` - عدد مرات الاستخدام
- `createdAt` - تاريخ الإنشاء

---

## 🔧 ملفات إضافية

### **todo.md**
قائمة المهام والميزات المكتملة:
- تتبع التقدم في التطوير
- سجل الميزات المنجزة
- خطط الميزات المستقبلية

### **seed-popular-prompts.mjs**
نص برمجي لملء قاعدة البيانات:
- إضافة البرومبتات الشعبية الأولية
- بيانات تجريبية للاختبار

### **.gitkeep**
ملفات فارغة للحفاظ على المجلدات في Git

### **.manus/**
مجلد خاص بمنصة Manus:
- `db/` - استعلامات قاعدة البيانات المحفوظة

---

## 🚀 كيفية التشغيل

### **التثبيت:**
```bash
cd raqim_ai_966
pnpm install
```

### **تشغيل الخادم التطويري:**
```bash
pnpm dev
```

### **دفع تغييرات قاعدة البيانات:**
```bash
pnpm db:push
```

### **بناء للإنتاج:**
```bash
pnpm build
```

---

## 📊 إحصائيات المشروع

- **إجمالي الملفات:** ~150+ ملف
- **اللغات المستخدمة:** TypeScript, TSX, CSS, SQL
- **المكونات:** 40+ مكون React
- **الصفحات:** 4 صفحات رئيسية
- **الجداول:** 3 جداول في قاعدة البيانات
- **الميزات الرئيسية:** 8 ميزات كبرى

---

## 🎯 الميزات الرئيسية

1. ✅ **أداة توليد البرومبتات الذكية** - مع 6 أنواع استخدام وخيارات متقدمة
2. ✅ **مكتبة القوالب الجاهزة** - 6 قوالب متنوعة
3. ✅ **القوالب المتخصصة** - 8 مجالات × 3 قوالب (24 قالب)
4. ✅ **البرومبتات الشعبية** - 8 برومبتات بتقييمات
5. ✅ **الأمثلة الحية** - 4 أمثلة واقعية
6. ✅ **محلل البرومبتات الذكي** - تحليل شامل مع اقتراحات
7. ✅ **المكتبة الشخصية** - حفظ وإدارة البرومبتات
8. ✅ **الوضع الداكن/الفاتح** - تبديل سلس بين الوضعين

---

## 🌐 التقنيات المستخدمة

### **Frontend:**
- React 19
- TypeScript
- Tailwind CSS 4
- shadcn/ui
- Lucide Icons
- Wouter (Routing)
- tRPC Client

### **Backend:**
- Node.js
- Express 4
- tRPC 11
- Drizzle ORM
- MySQL/TiDB

### **أدوات التطوير:**
- Vite
- pnpm
- ESLint
- TypeScript Compiler

---

## 📝 ملاحظات مهمة

1. **اتجاه RTL:** جميع الصفحات تدعم الاتجاه من اليمين لليسار للغة العربية
2. **الخطوط:** يستخدم المشروع خط Cairo من Google Fonts
3. **الأيقونات:** جميع الأيقونات من Lucide React
4. **التخزين:** LocalStorage للوضع الداكن/الفاتح، قاعدة البيانات للبرومبتات
5. **المصادقة:** Manus OAuth مدمج بالكامل
6. **الاستجابة:** تصميم متجاوب بالكامل (جوال، تابلت، كمبيوتر)

---

## 🔗 روابط مفيدة

- **الموقع المباشر:** يتم توفيره عبر منصة Manus
- **التوثيق:** README.md في المجلد الجذر
- **قائمة المهام:** todo.md

---

## 👨‍💻 معلومات التواصل

- **تيليجرام:** @dr_basl
- **تويتر:** @hzbr_al

---

**تم إنشاء هذا التوثيق بواسطة Manus AI**
**آخر تحديث:** نوفمبر 2025
